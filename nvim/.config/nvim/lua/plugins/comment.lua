local keymap = require('lib.utils').keymap

require('Comment').setup({
    padding = true,
    sticky = true,
    toggler = {
        line = '<C-_>'
    },
    opleader = {
        line = '<C-_>'
    }
})
